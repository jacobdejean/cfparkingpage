---
import Layout from "../layouts/Layout.astro";
import "./src/styles/global.css";

// Access environment variables
const title = import.meta.env.PUBLIC_PAGE_TITLE || "This Domain is Parked";
const caption =
    import.meta.env.PUBLIC_PAGE_CAPTION ||
    "The owner of this domain has not yet published their website. Please check back later.";
const copyright =
    import.meta.env.PUBLIC_PAGE_COPYRIGHT ||
    `© ${new Date().getFullYear()} All rights reserved`;
const email = import.meta.env.PUBLIC_EMAIL ?? "inbox@domain.com";
---

<Layout>
    <div
        class="flex flex-col items-center justify-center min-h-screen bg-gradient-to-b from-gray-50 to-gray-100 p-4 text-center"
    >
        <h1 class="text-4xl font-bold text-gray-800 mb-4">
            {title}
        </h1>
        <p class="text-xl text-gray-600 mb-8 max-w-md">
            {caption}
        </p>
        {
            email ? (
                <a
                    href={`mailto:inbox@domain.com`}
                    class="text-xl text-gray-600 mb-8 max-w-md underline"
                >
                    {email}
                </a>
            ) : null
        }

        <!-- Contact form coming soon -->
        <!-- <button
            class="flex items-center gap-2 text-blue-600 hover:text-blue-800 cursor-pointer"
            onclick="document.getElementById('contactDialog').showModal()"
        >
            Contact Domain Owner
        </button>

        <dialog
            id="contactDialog"
            class="p-6 rounded-lg shadow-xl backdrop:bg-gray-500/50 mx-auto my-auto"
        >
            <div class="w-full max-w-md">
                <h2 class="text-2xl font-bold mb-4">Contact Domain Owner</h2>
                <form method="dialog" class="space-y-4">
                    <div class="flex flex-col">
                        <label htmlFor="name" class="text-left mb-1">
                            Name
                        </label>
                        <input
                            type="text"
                            id="name"
                            name="name"
                            required
                            class="border rounded p-2"
                        />
                    </div>
                    <div class="flex flex-col">
                        <label htmlFor="email" class="text-left mb-1">
                            Email
                        </label>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            required
                            class="border rounded p-2"
                        />
                    </div>
                    <div class="flex flex-col">
                        <label htmlFor="message" class="text-left mb-1">
                            Message
                        </label>
                        <textarea
                            id="message"
                            name="message"
                            required
                            rows={4}
                            class="border rounded p-2"></textarea>
                    </div>
                    <input type="hidden" name="id" value={domainId} />
                    <div class="flex gap-2 justify-end">
                        <button
                            type="button"
                            class="px-4 py-2 border rounded hover:bg-gray-100"
                            onclick="document.getElementById('contactDialog').close()"
                        >
                            Cancel
                        </button>
                        <button
                            type="submit"
                            class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
                        >
                            Send Message
                        </button>
                    </div>
                </form>
            </div>
        </dialog> -->

        <p class="mt-8 text-sm text-gray-500">
            {copyright}
        </p>
    </div>
</Layout>
